# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules

# testing
/coverage

# production
web/build

# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

web/src/firebaseConfig.ts
web/src/config/members.ts

scripts/add_members_to_club/members.yaml
firebase_sa_key.json
database.rules.json

# Docker compose with secrets
docker-compose.local.yml

# Deployment configuration files (contain secrets)
**/.deploy-config
.deploy-config
.deploy-config-local

# Runtime config files (may contain sensitive service URLs)
web/build/config.js
web/public/config.js
config.js
**/config.js

# Docker entrypoint script (contains logic, but safe to commit)
# web/docker-entrypoint.sh is intentionally NOT ignored